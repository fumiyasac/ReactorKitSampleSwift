# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'ReactorKitSampleSwift' do
  use_frameworks!

  # Pods for ReactorKitSampleSwift
  pod 'ReactorKit'
end
